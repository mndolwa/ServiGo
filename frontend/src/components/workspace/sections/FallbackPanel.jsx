export default function FallbackPanel({ section }) {
  return (
    <article className="panel full">
      <h3>{section}</h3>
      <p>This workspace module is scaffolded and ready for additional business rules and reports.</p>
    </article>
  );
}
